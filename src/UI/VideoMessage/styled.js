import styled from 'styled-components'
import { Row } from '../Layout'

export const VideoMessageWrapp = styled.div`
  height: 120px;
  width: 100%;
  position: relative;
  border: 1px solid #fff;
`

export const VideoMessageBar = styled(Row)`
  position: absolute;
  bottom: 0;
  width: 100%;
  padding: 0 10px;
`
